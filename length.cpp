#include<iostream>
#include<string>
using namespace std;

int main()
{
    string s="nincompoop";
    cout<<"Length/size of string="<<s.size()<<endl;//used to find the length of string,we can also use s.length() that also gies the same value
    
   
    
    return 0;
}